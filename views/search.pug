extends layout

include mixins/forecast

block content
  .inner
    form(action=`/` method="POST" class="city")
      input(type="text" id="address" name="address" placeholder="Enter your city..." required)
      input(type="submit" value="Find" class="button")

    if current
      +forecast(current, tomorrow, three, four, five)